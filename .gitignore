__pycache__/
*.pyc
.venv/
# База с историей сделок — только локально, в репозиторий не попадает никогда
*.db
*.sqlite
/data/
# Страховка: ключи должны жить в Keychain, но если что-то создаст эти файлы —
# они не уедут в публичный репозиторий
.env
*.key
*secret*
